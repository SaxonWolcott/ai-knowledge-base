---
description: Ingest a source into the wiki (interactive)
---

Ingest the following source into the wiki by following the **Ingest** workflow in
`CLAUDE.md` (§8). This is **interactive**: read the source fully, then discuss the key
takeaways with me and wait for my direction **before writing any pages**.

If the source is a URL, `WebFetch` it and save a markdown copy to `raw/` first. If it's a
tweet, treat it as a credibility-weighted claim per the source-type table (§7) — do not
create a source page for it.

Source: $ARGUMENTS
